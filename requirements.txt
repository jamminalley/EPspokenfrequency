# Exact versions that produced the published out/. The build is
# deterministic for a given set of versions; a different simplemma, Stanza
# or Stanza model release can change individual lemmas.
#
#   python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
#   ./.venv/bin/python -c "import stanza; stanza.download('pt')"

stanza==1.14.0          # primary lemmatizer, and POS for participle splitting
torch==2.14.0           # required by stanza
simplemma==2.0.0        # fallback lemmatizer (dictionary gate, frequency tail)
pyspellchecker==0.9.0   # PT dictionary for the gate and filters; EN list for foreign words
regex==2026.9.10        # Unicode-aware tokenization
pyyaml==6.0.3           # config.yaml

# Tests only
pytest==9.1.1
